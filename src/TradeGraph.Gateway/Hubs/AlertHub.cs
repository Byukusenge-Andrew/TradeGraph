using Microsoft.AspNetCore.SignalR;

namespace TradeGraph.Gateway.Hubs;

public class AlertHub : Hub
{
    // Real-time connections come here, methods can be added if clients need to invoke server functions
}
